"""
if we want to save the plot instaed of checking it merely in VSC
we can replace  plt.show() with
plt.savefig('squares_plot.png', bbox_inches='tight')
第一个参数是保存名称， 第二个参数用于裁剪多余的空白
当然， 如果使用path对象, 我们可以将其保存在任意位置
"""